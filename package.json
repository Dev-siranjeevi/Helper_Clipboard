{
  "name": "One step Helper clipboard",
  "version": "4.2.1",
  "description": "Copy, paste,store and convert",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-win": "electron-packager . Clipboard --overwrite --asar=true --platform=win32 --arch=ia32 --icon=src/assets/icons/appIconV4.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Helper_Clipboard\""
  },
  "author": "One step",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.3.15",
    "electron-packager": "^15.5.1"
  }
}
